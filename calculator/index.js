const calculator = {
    result: 0,
    set: require('./set'),
    add: require('./add'),
    sub: require('./sub'),
    mult: require('./mult'),
    div: require('./div')
}

module.exports = calculator;